const loadingTitle = '끄적끄적 | 로딩 중...';
const defaultTitle = '끄적끄적 | 로그인';
const signUpTitle = '끄적끄적 | 회원가입';
const diaryTitle = '끄적끄적 | 내 다이어리';
const createTitle = '끄적끄적 | 오늘의 일기쓰기';

const titleConfigs = {
  loadingTitle,
  defaultTitle,
  signUpTitle,
  diaryTitle,
  createTitle,
};

export default titleConfigs;
